def solution(A,B):
    
    answer = 0
    
    A = sorted(A)
    B = sorted(B,reverse=True)
    
    sum = 0
    for i in range(len(A)):
        sum += A[i]*B[i]
    
    
    answer = sum

    return answer
