import math

def solution(left, right):
    answer = 0
    for i in range(left, right + 1, 1):
        sqrt = math.sqrt(i)
        print(sqrt)
        if int(sqrt) == sqrt:
            answer -= i
        else:
            answer += i

    return answer
